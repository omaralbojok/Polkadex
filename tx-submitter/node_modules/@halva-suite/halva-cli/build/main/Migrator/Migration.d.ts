import { Contract } from '../Deployer/Contract';
import { HalvaTestConfig } from '../TestRunner';
import { DeployData } from './DeployData';
export declare class Migration {
    data: DeployData;
    parent: Migration;
    private config;
    constructor(config: HalvaTestConfig);
    deploy(contract: MigrationContract, args?: any, constructorIndex?: number): Promise<Contract>;
}
export declare const RunMigration: (migrationsPath: string, config: HalvaTestConfig) => Promise<never>;
export interface MigrationContract {
    contractPath: string;
    AbiPath: string;
}
