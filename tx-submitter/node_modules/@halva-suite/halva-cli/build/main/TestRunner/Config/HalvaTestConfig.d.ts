import { MochaOptions } from 'mocha';
export declare class HalvaTestConfig {
    testingFiles: string[];
    mocha: MochaOptions;
    bail: boolean;
    halvaJs: any;
    colors?: boolean;
    networkName: string;
    timeout: number;
    networkList: any;
    verbose: boolean;
    types: any;
    mnemonic: string;
    constructor(filesPath: string[], mocha?: MochaOptions, network?: string, networkName?: string, bail?: boolean, timeout?: number, colors?: boolean, verbose?: boolean, isTest?: boolean);
}
