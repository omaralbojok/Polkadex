import { HalvaTestConfig } from '../TestRunner';
export declare const GetNetworks: (config: HalvaTestConfig) => NetworkGet[];
export declare const run: (config: HalvaTestConfig) => void;
interface NetworkGet {
    name: string;
    address: string;
    contracts: ContractGet[];
}
interface ContractGet {
    name: string;
    address: string;
    deployed: boolean;
}
export {};
