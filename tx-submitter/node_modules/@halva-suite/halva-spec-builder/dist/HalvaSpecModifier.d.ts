import Keyring from '@polkadot/keyring';
declare type Middleware<T> = (context: HalvaMiddlewareContext) => any;
export default interface HalvaMiddlewareContext {
    jsonSchema: any;
    ed25519Keys: Keyring;
    sr25519Keys: Keyring;
    customArgs: any;
    path: string;
    mnemonic: string;
    balance: string;
    count: number;
}
export declare class HalvaMiddlewareRunner {
    path: string;
    mnemonic: string;
    jsonSchema: any;
    balance: string;
    count: number;
    customArgs: any;
    ed25519Keys: Keyring;
    sr25519Keys: Keyring;
    middlewares: Middleware<HalvaMiddlewareContext>[];
    constructor(path: string, balance: string, count: number, customArgs?: any);
    apply(...middleware: Middleware<HalvaMiddlewareContext>[]): HalvaMiddlewareRunner;
    run(json?: string): Promise<HalvaMiddlewareRunner>;
    setMnemonic(mnemonic: string): HalvaMiddlewareRunner;
    output(path: string): void;
}
export declare class HalvaSpecModifier {
    static init(path: string, balance: string, count: number): HalvaMiddlewareRunner;
}
export {};
